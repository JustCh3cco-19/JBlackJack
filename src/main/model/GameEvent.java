package main.model;

/**
 * Represents the {@code GameEvent} enum.
 */
public enum GameEvent {
    /** The observable game state has changed. */
    STATE_CHANGED,

    /** The current round has ended. */
    GAME_OVER
}
